//  Frontend
    npm create vite@latest
    npm install -D tailwindcss postcss autoprefixer
    npx tailwindcss init -p
    npm i -D @types/node
    npx shadcn-ui@latest init
    yarn add zod

//  Do check typescript product deployment error before deploying in production env
    cmd: npx tsc
    if output comes then error there
    else no error